{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "hello-world (Debug)",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/hello-world/main.go"
        }
    ]
}
